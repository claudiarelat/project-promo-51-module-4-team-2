<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Proyecto creado</title>
  <link rel="stylesheet" href="/styles/style.css" />
  <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@700&display=swap" rel="stylesheet">


</head>
<body>

 <h1 class="titulo-seccion">DATOS DE LA OBRA</h1>

  <section class="detalle-proyecto">
    <!-- Portada de la película -->
    <% if (proyecto.imagen_obra) { %>
      <div class="portada-pelicula">
        <img src="<%= proyecto.imagen_obra %>" alt="<%= proyecto.titulo_obra %>">
      </div>
    <% } %>
      <div class="card-content">
        <div class="card-body">
        <!-- Columna izquierda: imagen personaje + datos -->
        <div class="left">
          <div class="image-container">
            <% if (proyecto.imagen_personaje) { %>
              <img src="<%= proyecto.imagen_personaje %>" alt="<%= proyecto.nombre_personaje %>">
            <% } %>
          </div>
          <div class="personaje-info">
            <h3><%= proyecto.nombre_personaje %></h3>
            <p><%= proyecto.rol_personaje %></p> 
          </div>
        </div>

        <!-- Columna derecha: info obra -->
        <div class="right">
          <% if (proyecto.titulo_obra) { %>
            <h3 class="titulo-obra"><%= proyecto.titulo_obra %></h3>
          <% } %>
          
          <% if (proyecto.frase_estrella) { %>
            <p class="frase">«<%= proyecto.frase_estrella %>»</p>
          <% } %>
          
          <% if (proyecto.sinopsis) { %>
            <p class="sinopsis"><%= proyecto.sinopsis %></p>
          <% } %>

          <div class="extra-info">
            <% if (proyecto.genero) { %>
              <p class="genero"> <%= proyecto.genero %></p>
            <% } %>
            
            <% if (proyecto.link_plataforma) { %>
              <a href="<%= proyecto.link_plataforma %>" target="_blank">🎬</a>
            <% } %>
            
            <% if (proyecto.link_resenas) { %>
              <a href="<%= proyecto.link_resenas %>" target="_blank">⭐⭐⭐⭐⭐</a>
            <% } %>
          </div>
        </div>
        </div>
      </div>
    </section>
</body>
</html>


