<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Proyecto creado</title>
  <link rel="stylesheet" href="/styles/style.css" />
</head>
<body>
  <h1>Datos del proyecto</h1>
  <section>
    <div>
       <!-- Imagen de la peli -->
        <% if (proyecto.imagen_obra) { %>
          <img src="<%= proyecto.imagen_obra %>" alt="<%= proyecto.titulo_obra %>">
        <% } %>
    </div>
    <div>
      <div>
        <!-- Imagen del personaje -->
        <% if (proyecto.imagen_personaje) { %>
          <img src="<%= proyecto.imagen_personaje %>" alt="<%= proyecto.nombre_personaje %>" style="max-width:200px;">
        <% } %>
      </div>

      <div>
        <h3><%= proyecto.nombre_personaje %></h3>
        <p><%= proyecto.rol_personaje %></p> 
      </div>

      <div>
        <h3><%= proyecto.titulo_obra %></h3>
        <p><%= proyecto.frase_estrella %></p>
        <p><%= proyecto.sinopsis %></p>
        <div>
          <p><%= proyecto.genero %></p>
          
          <% if (proyecto.link_plataforma) { %>
            <a href="<%= proyecto.link_plataforma %>" target="_blank">Ver en plataforma</a>
          <% } %>
          
          <% if (proyecto.link_resenas) { %>
            <a href="<%= proyecto.link_resenas %>" target="_blank">Ver reseñas</a>
          <% } %>
        </div>         
      </div>
    </div>
  </section>
</body>
</html>

